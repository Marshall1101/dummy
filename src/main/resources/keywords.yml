  "{{ azure. }}"
    "{{ ansible_ssh_user }}"
    "{{ ansible_ssh   }}"
    "{{ openstack_username }}"
    "{{ openstack_password }}"
    "{{ tdsp_encryption_key }}"
    "{{ vault_test_client_secret }}"
    "{{ vault_client_secret }}"
    "{{ vault_cert_pwd }}"
    azure_subscription_id       = "{{ azure_subscription_id }}"
    db_admin_username           = "{{ db_admin_username }}"
    db_admin_password           = "{{ db_admin_password }}"

    "{{ aws_access_key }}"
    "{{ aws_secret_key }}"
    "{{ aws_region | default('eu-west-1') }}"
    "{{ aws_security_group | default ('default')}}"
    "{{ aws_ami_id | default('ami-02724d1f') }}"

    "{{ download.password | default(omit) }}"
    "{{disablePasswordAuthentication}}"
    "{{ vm_list_cmd.stdout }}"
    "{{ vm_ip_azure_cmd.stdout }}"
    "{{ vm_vault_cmd.stdout }}"
    "{{ lb_pubip_cmd.stdout }}"
    "{{ azure_virtual_network_name | default('KubeVNET') }}"
    "{{ azure_subnet_admin_name | default('ad-subnet') }}"
    "{{ azure_subnet_masters_name | default('master-subnet') }}"
    "{{ azure_subnet_minions_name | default('minion-subnet') }}"
    "{{ azure_route_table_name | default('routetable') }}"
    "{{ azure_security_group_name | default('secgroup') }}"
    "{{ cluster_name }}"
    "{{imageReference|to_json}}"
    "{{ azure_storage_account_type | default('Standard_LRS') }}"
    "{{ base_dir }}"
    "{{ item }}"
    "{{availabilitySetMasters}}"
    "{{apiVersion}}"
    "{{faultDomainCount}}"
    "{{updateDomainCount}}"
    "{{availabilitySetMinions}}"
    "{{apiVersion}}"
    "{{faultDomainCount}}"
    "{{updateDomainCount}}"
    "{{apiVersion}}"
    "{{bastionIPAddressName}}"
    "{{ bastion_domain_prefix }}"
    "{{apiVersion}}"
    "{{apiVersion}}"
    "{{bastionVMName}}"
    "{{bastionVmSize}}"
    "{{bastionVMName}}"
    "{{admin_username}}"
    "{{admin_password}}"
    "{{sshKeyPath}}"
    "{{key}}"
    "{{apiVersion}}"
    "{{apiVersion}}"
    "{{kube_apiserver_port}}"
    "{{kube_apiserver_port}}"
    "{{kube_apiserver_port}}"
    "{{apiVersion}}"
    "{{apiVersion}}"
    "{{apiVersion}}"
    "{{masters_vm_size}}"
    "{{admin_username}}"
    "{{admin_password}}"
    "{{disablePasswordAuthentication}}"
    "{{sshKeyPath}}"
    "{{key}}"
